# Code Review — qwen-agent-tui

> Generated by NanoAgent on 2026-05-29. Covers core agent logic, OpenTUI frontend, MCP integration, security/permissions, and test quality.
>
> **Status (2026-07-29):** All Critical, High, Medium, and Low items addressed (or confirmed already resolved / intentional).

---

## Critical

### 1. `src/agent/run.ts` — Unbounded `while(true)` loop with no yield — FIXED

Yield (`await setTimeout(0)`) added after each tool round and on reasoning-only continue paths.

---

### 2. `src/mcp/index.ts` — `callTool` does not propagate `AbortSignal` — FIXED

Passes `{ signal }` as SDK `RequestOptions`; abort maps to `{ ok: false, error: 'Aborted' }`.

---

### 3. `src/opentui/app.tsx` — Error catch block does not reset agent state — FIXED

`handleSubmit` catch calls `agent.setState('idle')` and syncs the store on error and abort.

---

## High

### 4. `src/agent/run.ts` — Duplicate `maxRounds` / `maxIterations` — FIXED

Single `turnLimit = min(maxIterations, maxRounds)` check.

---

### 5. `src/security/permissions.ts` — `mcp_*` auto-allow — DOCUMENTED (intentional)

Trusted MCP servers auto-allow except in `read_only`. Documented in `/permissions`; override with `/permissions deny mcp_…`.

---

### 6. `src/agent/core.ts` — `toolSchemaCache` key — FIXED

Cache key includes tool names; `invalidateToolSchemaCache()` after MCP register.

---

### 7. `src/opentui/app.tsx` — `handleSubmit` error path — FIXED

Same as #3.

---

## Medium

### 8. `src/opentui/skills-overlay.tsx` — Focus indices out of sync — FIXED

Focus clamped with `CREATE_FOCUS_MAX` / `INSTALL_FOCUS_MAX`; `resetFormState()` on cancel/Esc/mode exit.

---

### 9. `src/opentui/app.tsx` — `onUpdate` during partial init — FIXED

`onUpdate` gated behind `initDone` until `agent.init()` settles; welcome banner still syncs directly.

---

### 10. `src/mcp/index.ts` — Silent Streamable HTTP → SSE fallback — FIXED

Combined HTTP+SSE errors on failure; debug log when SSE fallback succeeds (`QWEN_DEBUG_MCP`).

---

### 11. `src/agent-subagents.ts` — Sub-agent result name uses ID — FIXED

`subAgentDisplayName(prompt)` used on error paths and when result name is `sa-*` / `pool`.

---

### 12. `src/opentui/command-dropdown.tsx` — `getActualIndex` O(n²) — FIXED

`commandIndex` attached when building `displayItems`; selection/scroll/pick are O(1).

---

## Low

### 13. `src/agent/run.ts` — `MAX_REASONING_ONLY` hardcoded — FIXED

Configurable via `cfg.maxReasoningOnlyRounds` (default 5); validated in `config/validate.ts`.

---

### 14. `src/opentui/app.tsx` — F2 incomplete clear — FIXED

F2 also clears `todos` and `toolResults`.

---

### 15. `src/agent-loop.test.ts` — Missing error-path tests — FIXED

Added tests for HTTP 500 responses and unreachable endpoints.

---

### 16. `src/opentui/slash-commands/permissions.ts` — Trailing double newline — FIXED

MCP line + help text cleaned up earlier.

---

### 17–20. Stub files — ALREADY RESOLVED

`src/agent/agent.ts` and `src/agent-tools/worker/*` are gone. `src/agent.ts` and `src/agent-tools/index.ts` are real re-exports (non-empty).

---

## Summary

| Severity | Count | Status |
|----------|-------|--------|
| Critical | 3 | Fixed |
| High | 4 | Resolved (1 intentional/documented) |
| Medium | 5 | Resolved |
| Low | 8 | Resolved / already gone |
